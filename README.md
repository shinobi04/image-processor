# Invoice Preprocessing API

FastAPI service that accepts images and PDFs of invoices, cleans and normalizes them, and returns a single OCR-optimized PDF (grayscale with contrast enhancement).

See `ENVIRONMENT.md` for environment setup instructions.
